const http = require('http'); const req = http.request({ hostname: 'localhost', port: 5000, path: '/api/users/login', method: 'POST', headers: { 'Content-Type': 'application/json' } }, res => { let data = ''; res.on('data', c => data += c); res.on('end', () => { const token = JSON.parse(data).token; const req2 = http.request({ hostname: 'localhost', port: 5000, path: '/api/invoices/5', method: 'PUT', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token } }, res2 => { let data2 = ''; res2.on('data', c => data2 += c); res2.on('end', () => console.log('Response:', data2)); }); req2.write(JSON.stringify({ items: [{ description: 'Test', quantity: 1, unit_price: 10 }], issue_date: '2026-08-07T19:00:00.000Z', due_date: '2026-08-13T19:00:00.000Z' })); req2.end(); }); }); req.write(JSON.stringify({ email: 'admin@adwiselabs.com', password: 'password123' })); req.end();
